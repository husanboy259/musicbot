services:
  - type: web
    name: musicbot
    env: node
    buildCommand: npm install && bash build.sh
    startCommand: npm start
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
        optional: true
